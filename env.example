# Copy the needed values into Vercel → Project → Settings → Environment Variables.
# (This file is just a reference — never commit real keys.)

# Anthropic API key for the AI chat endpoint (api/chat.js)
ANTHROPIC_API_KEY=sk-ant-...
